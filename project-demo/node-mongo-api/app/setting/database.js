
/* ======================================================================
 * Config Database - RESTful API
 * ====================================================================== */

var service = require ('./service');

/* ======================================================================
 * Export
 * ====================================================================== */

module.exports = {
    
    /* MongoDB 3.4 */
    'mongodb': 'mongodb://' + service.db_host + ': ' + service.db_port + '/' + service.db_name
    
};